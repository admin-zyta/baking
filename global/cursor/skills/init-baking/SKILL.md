---
name: init-baking
description: Alias of init-memory — bootstrap AGENTS + Engram. Use /init-memory or baking init-memory.
disable-model-invocation: false
user-invocable: true
---

# init-baking → use **init-memory**

Per-project init is now **`/init-memory`** (Baking 1.6+).

```bash
baking init-memory
```

See the **`init-memory`** skill and `~/.cursor/opus-sonnet/INIT-MEMORY.md`.

Global Baking config is still at `~/.cursor/opus-sonnet/config.json`. Auto-route toggle: `baking auto-route on`.
